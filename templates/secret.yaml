apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  namespace: {{ .Values.namespace }}
data:
  TELOXIDE_TOKEN: "{{ .Values.secret.telegramToken }}"
  API_TOKEN: "{{ .Values.secret.apiToken }}"
